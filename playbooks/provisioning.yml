---
- hosts: servers
  become: yes
  roles:
    - { role: base, tags: ['base'] }
    - { role: mattermost, tags: ['mattermost', 'matterbridge'] }
    - { role: motd, tags: ['motd'] }
    - { role: directory, tags: ['directory'] }
